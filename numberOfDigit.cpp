#include <iostream>
using namespace std;

int main()
{
    string myName;
    getline(cin,myName);
    //cout<<"my name is "<<myName<<endl;
    int x = 1;
    int y = 2;
   // cout<<(x>y);
   // short hand if else

    //string distance = (x<=1)?"can be travelled " : "cannot be travelled";
    //cout<<distance;
    int i=5;
    do{
      cout<<i<<endl;
      i++;
    }while(i<5);
}